#include "main.h"

/**
 * mul - multiplies two integers
 * @a: the 1st number
 * @b: the 2nd number
 *
 * Return: the product of a and b
 */
int mul(int a, int b)
{
	int product;

	product = a * b;

	return (product);
}
