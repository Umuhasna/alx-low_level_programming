#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Print some numbers
 *
 * Return: Always 0
 */
int main(void)
{
	int num;

	for (num = '0'; num <= '9'; num++)
	{
		putchar(num);
	}
	putchar(num);
	return (0);
}
