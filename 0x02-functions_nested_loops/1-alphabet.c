#include "main.h"

/**
 * main - Entry point
 *
 * Description: Prints the alphabet, in lowercase, followed by a newline.
 *
 * Return: Always 0
 */
int main(void)
{
	print_alphabet();
	_putchar(c = 'a', c <= 'z', c++);

	return (0);
}
