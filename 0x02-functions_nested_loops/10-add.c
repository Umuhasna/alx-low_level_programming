#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: the 1st number
 * @b: the 2nd number
 *
 * Return: the result of the addition
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
