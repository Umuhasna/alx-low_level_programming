#include "main.h"

/**
 * _abs - Entry point
 *
 * Description: computes the absolute value of an integer
 * @int: the parameter
 * Return: 0
 */
int _abs(int)
{
	int i;

	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (-i);
	}
}
